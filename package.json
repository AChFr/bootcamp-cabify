{
  "name": "exercise01-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "mocha tests/**/*.js && node server.js",
    "dev": "nodemon server.js",
    "test": "mocha tests/**/*.js"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "chai": "^4.3.6",
    "express": "^4.18.1",
    "mocha": "^10.0.0",
    "supertest": "^6.2.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}